services:
  chatterbox:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chatterbox-tts
    ports:
      - "7860:7860"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    # Uncomment the lines below if you want to allow GPU access (requires Docker with NVIDIA Container Toolkit)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - capabilities: [gpu]
    #           driver: nvidia

